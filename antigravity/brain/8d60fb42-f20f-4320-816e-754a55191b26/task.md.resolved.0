# Tarefa: Integração Senior na Precificação (com Cache Local)

## Backend (Program.cs)
- [x] Criar tabela local `FacilitiesJobs` (CODCAR, ESTCAR, TITCAR, SalarioBase, UpdatedAt)
- [ ] Criar endpoint `POST /api/jobs/sync-facilities-jobs` → consulta Senior, salva local, loga em JobLogs
- [ ] Criar endpoint `GET /api/facilities-jobs` → lê da tabela local (sem chamar Senior)

## Frontend (precificacao.html)
- [ ] Trocar campo de texto "Cargo" por `<select>` carregado de `/api/facilities-jobs`
- [ ] Ao selecionar o cargo, auto-preencher o campo "Salário Base"
- [ ] Adicionar badge visual "🔗 Senior Cache" nos campos preenchidos automaticamente
- [ ] Manter todos os outros campos (encargos, benefícios, impostos) editáveis com padrões

## Rotinas (rotinas.html)
- [ ] Verificar se a tela de rotinas já exibe o job de sync, e adicionar `sync-facilities-jobs` se não tiver

## Verificação
- [ ] Testar endpoint /api/jobs/sync-facilities-jobs e confirmar dados no SQL local
- [ ] Testar /api/facilities-jobs retornando dados do cache
- [ ] Testar fluxo completo no browser  
